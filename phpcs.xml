<?xml version="1.0"?>
<ruleset name="Event Espresso Addon">
    <description>Event Espresso Addon rules for PHP_CodeSniffer</description>

    <file>./</file>

    <arg name="extensions" value="php"/>
    <arg value="sp"/>
    <arg name="colors" />

    <rule ref="EventEspresso" />

    <!-- file exclusions -->
    <exclude-pattern>isdk.php</exclude-pattern>

    <!-- specific rule exclusions per file -->
    <rule ref="PSR1.Files.SideEffects.FoundWithSymbols">
        <exclude-pattern>eea-people-addon.php</exclude-pattern>
    </rule>

    <rule ref="PSR1.Classes.ClassDeclaration.MissingNamespace">
        <exclude-pattern>EE_People_Config.php</exclude-pattern>
    </rule>

    <rule ref="Squiz.Classes.ValidClassName.NotCamelCaps">
        <exclude-pattern>EE_People_Config.php</exclude-pattern>
    </rule>
</ruleset>
